# retail-sales-data-pipeline
I built an end-to-end retail data pipeline using Databricks and Snowflake.  I ingested raw data, cleaned and standardized it in the Silver layer, then created Gold-level fact and KPI tables like total sales and average order value.  Finally, I loaded the data into Snowflake and used SQL for analytics.
